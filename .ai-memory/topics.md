# Topics

## Hermes voice bridge

The `--hermes` CLI preset connects local speech recognition to Hermes' OpenAI-compatible Chat Completions API. Windows uses ChatTTS on CPU; other platforms use Qwen3-TTS. Hermes tool progress stays server-side and the bridge speaks the final answer.
